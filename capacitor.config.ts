import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'webnativex.app',
  appName: 'webvativex.app',
  webDir: 'dist/web-native-app/browser'
};

export default config;
